function range(from, to) {
  let arr = []
  for(let i = from ;i<=to;i++){ //just iterating from starting to end number and pusing all the numbers from the starting point to the endpoint point into the arr
    arr.push(i)
  }
  return arr
}
